{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "DEPRECATED: The name and id of a (shelf) location, the forth-level location unit. This is a read-only proxy to the new locations interface at /locations that should be used instead.",
  "properties": {
    "id": {
      "description": "UUID of the (shelf) location",
      "type": "string"
    },
    "name": {
      "description": "Name of the (shelf) location",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "name"
  ]
}
