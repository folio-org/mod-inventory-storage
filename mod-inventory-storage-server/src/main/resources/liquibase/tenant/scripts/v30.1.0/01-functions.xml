<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd"
                   objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

  <changeSet id="MODINVSTOR-1051@@schema-create-function-to-timestamp" author="folio">
    <comment>Create immutable to_timestamp helper function for indexing</comment>
    <sql splitStatements="false">
      CREATE OR REPLACE FUNCTION to_timestamp(text)
        RETURNS timestamp AS $$
      BEGIN
        -- We assume the input text is always in a format (like ISO 8601)
        -- that casts consistently to timestamp. This function turns the
        -- ::timestamp cast into an IMMUTABLE function, so that it can be
        -- used in indexes
        RETURN $1::timestamp;
      EXCEPTION WHEN others THEN
        RETURN NULL;
      END;
      $$ LANGUAGE plpgsql IMMUTABLE;
    </sql>
  </changeSet>

  <changeSet id="MODINVSTOR-1051@@schema-create-function-jsonb-array-index-helpers" author="folio" runOnChange="true">
    <comment>Create JSONB array index helper functions</comment>
    <sql splitStatements="false">
      -- Convert text to lowercase and remove accents for consistent indexing
      -- Usage: normalize_token('Sømé Téxt wîth Åcçéñts') -> 'some text with accents'
      CREATE OR REPLACE FUNCTION normalize_token(input_text text)
        RETURNS text AS
      $$
      SELECT lower(${database.defaultSchemaName}.f_unaccent(input_text))
      $$ LANGUAGE sql IMMUTABLE
                      PARALLEL SAFE;

      -- Normalize each element in a JSONB array of strings and aggregate back into a JSONB array
      -- Usage: normalize_jsonb_array('["Sømé", "Téxt", "wîth", "Åcçéñts"]'::jsonb) -> '["some", "text", "with", "accents"]'
      CREATE OR REPLACE FUNCTION normalize_jsonb_array(input_array jsonb)
        RETURNS jsonb AS
      $$
      SELECT coalesce(jsonb_agg(${database.defaultSchemaName}.normalize_token(val)), '[]' ::jsonb)
      FROM jsonb_array_elements_text(input_array) AS val
      $$ LANGUAGE sql IMMUTABLE
                      PARALLEL SAFE;

      -- Extract values from a JSONB array of objects based on specified keys and aggregate into a JSONB array
      -- Usage: extract_values('{"items": [{"name": "Item1", "value": "A"}, {"name": "Item2", "value": "B"}]}'::jsonb, 'items', 'value') -> '["A", "B"]'
      CREATE OR REPLACE FUNCTION extract_values(input_jsonb jsonb, array_key text, value_key text)
        RETURNS jsonb AS
      $$
      SELECT coalesce(jsonb_agg(elem -> value_key), '[]'::jsonb)
      FROM jsonb_array_elements(input_jsonb -> array_key) AS elem
      $$ LANGUAGE sql IMMUTABLE
                      PARALLEL SAFE;
    </sql>
  </changeSet>

</databaseChangeLog>
