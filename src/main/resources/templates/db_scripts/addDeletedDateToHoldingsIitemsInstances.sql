alter table ${myuniversity}_${mymodule}.instance add "deletedDate" timestamp;
alter table ${myuniversity}_${mymodule}.item add "deletedDate" timestamp;
alter table ${myuniversity}_${mymodule}.holding_record add "deletedDate" timestamp;
